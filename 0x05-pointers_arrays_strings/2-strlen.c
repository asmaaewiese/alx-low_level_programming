#include "main.h"
/**
 * _strlen - returns the length of a string
 *@s: strinrg
 * Return: length
 */
int _strlen(char *s)
{       
        int wenth =0;

	while (*s != '\0')
	{
		wenth++;
		s++;
	}

	return (wength";
}
